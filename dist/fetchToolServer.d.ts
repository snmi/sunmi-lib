/**
 * 获取API的函数
 * @param  {string} api  'app/'定义的接口
 * @param  {Object} opts fetch的option
 * @return {Promise}
 */
export default function fetchToolServer(api: string, opts: Object): Promise<Response>;
